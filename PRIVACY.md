# 🔒 Smart Bookmark 隐私政策 (Privacy Policy)

**生效日期**：2026 年 9 月 14 日  
**最近更新**：2026 年 9 月 14 日  
**语言版本**：简体中文 | [English](./PRIVACY_EN.md)

---

## 概述与核心承诺

感谢您使用 **Smart Bookmark（智能书签与新标签页扩展）**。我们深知书签、访问记录和内网网络拓扑属于高度敏感的个人与组织隐私。因此，Smart Bookmark 从架构设计的第一天起即全面恪守 **「本地优先（Local-First）」** 准则。

**我们的核心隐私承诺：**
- **零中心化服务器**：官方不运营、不部署任何用于收集或同步用户数据的云端服务器。
- **不收集个人身份信息 (PII)**：我们不要求用户注册账号，不收集姓名、邮箱、电话、设备硬件指纹等任何个人身份数据。
- **不进行用户行为追踪**：扩展内部无任何第三方统计埋点（如 Google Analytics、百度统计等），不追踪用户的浏览记录与习惯。
- **绝不出售或转让数据**：您的数据 100% 属于您，绝不被商业化利用。

---

## 1. 我们处理的数据及存储方式

### 1.1 书签、分组与标签数据
- **数据内容**：用户主动录入、导入或整理的书签名称、访问入口地址（URL）、自定义分组、标签（Tags）、网站 Favicon 图标。
- **存储机制**：**100% 存储于用户本地浏览器的 IndexedDB 数据库（基于 Dexie.js 引擎）**。数据在用户的设备本地持久化，除非用户主动导出备份或主动调用第三方 AI 整理，否则绝不会离开本地设备。

### 1.2 访问频次与点击统计（热度分析）
- **数据内容**：用户点击书签直达的本地累计计数、最后访问时间及近 7 天访问频次。
- **处理目的**：仅用于主页「常用」动态高频分组的本地即时计算，以及主页按访问热度排序。
- **存储机制**：完全保存在本地 IndexedDB 中，用户可在「访问热度与数据统计」面板中随时一键清空。

### 1.3 本地网络拓扑信息（WebRTC 局域网 IP 探测）
- **数据内容**：扩展使用 WebRTC API（`RTCPeerConnection`）在用户本机探测局域网内网 IPv4 地址（如 `192.168.x.x`、`10.x.x.x`）。
- **处理目的**：作为核心 **32 位二进制 XOR 最长公共前缀寻径算法** 的计算入参，用于判断当前客户端所处的内网网段，以便秒级自动优选距离最近的内网服务入口。
- **安全保障**：**探测过程完全在本地内存中执行，仅参与寻径匹配计算，绝不上报给任何远程服务器，绝不用于生成用户追踪指纹。**

### 1.4 可达性探针与响应测速结果
- **数据内容**：向用户配置的书签目标地址发送并发轻量级网络探测请求（HEAD/GET），获取 HTTP 状态码与往返延迟（ms）。
- **处理目的**：用于识别多入口可用性并在主入口宕机时毫秒级顺位降级，以及支持「按响应测速」排序。
- **存储机制**：仅保存在运行时的本地内存缓存中，设有生存周期（TTL），过期自动销毁。

---

## 2. 浏览器权限使用说明 (Permissions Disclosure)

为了实现核心功能，Smart Bookmark 在 `manifest.json` 中声明了以下权限。每项权限的申请均严格遵循最小必要原则：

| 权限名称 | 声明级别 | 申请目的与实际用途 | 隐私安全边界 |
| :--- | :--- | :--- | :--- |
| `storage` / `unlimitedStorage` | 必需 | 存储用户界面偏好配置，并允许 IndexedDB 存储大量书签、矢量图标缓存与容灾快照。 | 仅写入本机存储，不向外部网络发送。 |
| `bookmarks` | 可选 (Optional) | 仅在用户点击「导入浏览器书签」时按需请求，用于将原生书签导入为 Smart Bookmark。 | 仅在用户明确授权后一次性在本地读取，绝不上报或篡改浏览器原生书签。 |
| `<all_urls>` (host_permissions) | 必需 | ① 执行书签入口的连通性测速探针；<br>② 抓取目标网站的 Favicon 图标；<br>③ 转发用户自行配置的第三方 AI 接口（OpenAI/DeepSeek 等）。 | **绝不读取、注入、篡改或截获任何目标网页的网页内容、DOM、表单输入或 Cookie。** 仅发起轻量级连通性探测。 |
| `nativeMessaging` | 必需 | 与用户本机安装的 MCP（Model Context Protocol）本地服务进行进程间通信，支持外部 AI IDE（如 Cursor、Claude Desktop）联动。 | 仅限用户本机 IPC 进程通信，不连通任何外部未知服务。 |
| `activeTab` | 必需 | 当用户点击浏览器工具栏的扩展图标呼出快捷弹窗时，读取当前活动标签页的 URL 和标题，方便一键快捷添加为书签。 | 仅在用户主动点击扩展图标的瞬间对当前标签生效，绝不在后台监控活动标签。 |
| `alarms` | 必需 | 触发后台定时维护任务：清理过期的探针内存缓存、执行每日自动快照保留清理。 | 仅用于触发内部定时器，不涉及网络通信。 |
| `offscreen` | 必需 (Chromium) | 创建安全的后台离线文档环境，用于处理 Service Worker 无法直接执行的 DOM 图标解析与离线计算。 | 运行于完全隔离的沙箱上下文，无数据外泄。 |

---

## 3. 第三方服务与外部数据交互

Smart Bookmark 本身不包含任何追踪类第三方 SDK。仅在以下用户明确触发的场景下存在外部网络通信：

### 3.1 AI 智能整理功能（用户自选）
- **网页大模型免 Key 模式（默认推荐）**：  
  扩展在本地将书签的标题与 URL 组装为结构化 Prompt。**由用户手动复制并在第三方网页大模型（如 ChatGPT、Claude、DeepSeek、Kimi 等）中自行粘贴**。此过程中数据流向完全由用户手工掌控。
- **API 直连模式**：  
  仅当用户在「设置 → AI 模型」中**主动配置**了第三方 API Key（如 OpenAI、DeepSeek）并点击执行时，扩展才会将待整理书签的标题、URL 和现有分组信息发送给该 API 服务商。数据处理遵循对应服务商的隐私协议。扩展仅在本地存储 API Key，绝不上报至任何中间服务器。
- **本地 Ollama 模式**：  
  所有请求均发送至用户本地端点（如 `http://localhost:11434`），**100% 局域网离线运行，零外部网络传输**。

### 3.2 搜索引擎直达
主页聚合搜索框支持一键调用 Google、Bing、百度、GitHub 等主流搜索引擎。该行为等同于用户在浏览器地址栏直接发起搜索，由浏览器直接跳转至对应搜索引擎结果页，适用各搜索引擎自身的隐私政策。

### 3.3 网站图标（Favicon）
扩展优先使用内置离线矢量图标库（Simple Icons 3000+）。若无法匹配，会通过探针请求目标站点的 `favicon.ico` 或静态资源。所有获取到的图标均作为本地 Blob/Base64 缓存在 IndexedDB 中，避免重复请求。

---

## 4. 用户的数据控制权与留存期限

用户对自身的所有数据享有完全的自主控制权：

1. **导出与迁移权**：用户可随时通过「快照备份与数据管理」将全量书签、分组、标签、配置导出为标准 JSON 格式明文备份文件。
2. **版本回滚权**：系统在关键操作前自动生成本地安全快照，用户可随时一键回滚历史版本。
3. **彻底删除与销毁权**：
   - 用户可随时在「设置 → 数据重置」中执行「彻底清空所有数据」，此操作将物理清空本地 IndexedDB 中的全部数据表。
   - 用户随时可以在浏览器扩展管理界面（`chrome://extensions`）中**卸载**本扩展，卸载后浏览器将自动物理销毁该扩展占用的所有本地存储数据。

---

## 5. 儿童隐私保护 (Children's Privacy)

Smart Bookmark 是一款面向软件研发人员、系统运维与重度效率用户的生产力工具，不以 13 周岁以下儿童为目标受众，亦绝不故意收集任何儿童的个人信息。

---

## 6. 政策变更说明

随着浏览器扩展规范（如 Manifest 规范演进）或新特性的上线，我们可能会适时修订本隐私政策。任何实质性变更均会在 GitHub 仓库的 Release Notes 中公示，并更新文首的「最近更新」日期。

---

## 7. 联系方式与开源监督

本扩展完全开源，源码公开透明，接受全球开发者与用户的安全监督。若您对本隐私政策或扩展的数据处理机制有任何疑问、意见或漏洞报告，请通过以下方式联系：

- **开源仓库**：[https://github.com/ludengke95/smart-bookmark](https://github.com/ludengke95/smart-bookmark)
- **问题反馈**：[GitHub Issues](https://github.com/ludengke95/smart-bookmark/issues)
- **开发者邮箱**：ludengke95@gmail.com
