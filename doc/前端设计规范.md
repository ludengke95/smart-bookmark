# Smart Bookmark 智能书签 —— 极简主义前端设计规范 (Minimalist Design System Spec)

> **文档版本**：v1.0 (Minimalist Edition)  
> **归档位置**：`doc/前端设计规范.md`  
> **适用范围**：新标签页管理端 (`newtab`)、快捷弹窗 (`popup`)、通用交互组件与弹窗  
> **核心定位**：极速、沉静、高信息效率的个人新标签页与多入口智能寻径扩展  
> **设计主张**：*“新标签页是思维的发射台，而非喧闹的控制室。”*

---

## 目录

- [1. 设计哲学与核心原则](#1-设计哲学与核心原则)
- [2. 色彩系统与 3 套极简主题 (Color Tokens)](#2-色彩系统与-3-套极简主题-color-tokens)
  - [2.1 极简主题定义](#21-极简主题定义)
  - [2.2 状态与网络拓扑语义色 (Semantic Micro Colors)](#22-状态与网络拓扑语义色-semantic-micro-colors)
  - [2.3 完整的 CSS 自定义属性变量表 (CSS Variables)](#23-完整的-css-自定义属性变量表-css-variables)
- [3. 字体排版与网格标尺 (Typography & Spacing)](#3-字体排版与网格标尺-typography--spacing)
  - [3.1 字体栈定义](#31-字体栈定义)
  - [3.2 模块化字阶表 (Type Scale)](#32-模块化字阶表-type-scale)
  - [3.3 间距网格系统 (Spacing & Grid)](#33-间距网格系统-spacing--grid)
- [4. 界面版式架构与 ASCII 原型 (Layout Architecture)](#4-界面版式架构与-ascii-原型-layout-architecture)
  - [4.1 新标签页总体布局 (Desktop NewTab)](#41-新标签页总体布局-desktop-newtab)
  - [4.2 快捷弹窗布局 (Popup)](#42-快捷弹窗布局-popup)
- [5. 核心组件精细化设计规范 (Component Specifications)](#5-核心组件精细化设计规范-component-specifications)
  - [5.1 顶部静音导航条 (Top Navigation Bar)](#51-顶部静音导航条-top-navigation-bar)
  - [5.2 极简中心搜索条 (Search Capsule)](#52-极简中心搜索条-search-capsule)
  - [5.3 签名级特色组件：精密寻径书签卡片 (The Precision Route Card)](#53-签名级特色组件精密寻径书签卡片-the-precision-route-card)
  - [5.4 分组与标签栏 (Groups & Tag Pills)](#54-分组与标签栏-groups--tag-pills)
  - [5.5 弹窗与抽屉规范 (Modal & Sheet Design)](#55-弹窗与抽屉规范-modal--sheet-design)
  - [5.6 提示反馈 (Toast & Empty States)](#56-提示反馈-toast--empty-states)
  - [5.7 自适应表单选择器规范 (Adaptive Form Select & Segmented Control)](#57-自适应表单选择器规范-adaptive-form-select--segmented-control)
- [6. 交互动效与微反馈 (Motion & Micro-interactions)](#6-交互动效与微反馈-motion--micro-interactions)
- [7. 现有代码重构与落地对照表 (Refactoring Checklist)](#7-现有代码重构与落地对照表-refactoring-checklist)

---

## 1. 设计哲学与核心原则

针对原版本“视觉元素过多、高饱和色彩与阴影堆叠、缺乏呼吸感”的问题，本规范推倒重来，确立**极简主义（Minimalism & Swiss Style）**核心设计语言：

```
       [ 繁复的旧设计 ]                           [ 极简新规范 ]
 ┌─────────────────────────┐               ┌─────────────────────────┐
 │ 复杂渐变 + 粗厚彩色边框  │    ───► 削减    │ 单色极细边框 (0.5-1px)   │
 │ 霓虹发光 + 大面积重阴影  │    ───► 沉静    │ 自然柔和微阴影 / 纯扁平   │
 │ 常驻跳动大绿点 + 粗体徽章│    ───► 隐形    │ 等宽精密字符 + 静音指示点│
 │ 拥挤的顶部工具栏彩色按钮│    ───► 留白    │ 单色极简图标 + 悬浮响应  │
 └─────────────────────────┘               └─────────────────────────┘
```

### 三大核心法则

1. **绝对克制 (Radical Restraint)**
   - **界面颜色严格控制在 3 层**：背景底色（Canvas）、表面容器（Surface）、单一强调色（Accent）。
   - **摒弃装饰性渐变与荧光色**：不使用花哨的彩虹渐变和高光背景，界面的视觉权重完全由**字阶比例、留白（Negative Space）与线条密度**构建。
2. **静默智能 (Quiet Intelligence)**
   - 双网寻径（内网/外网）与延迟探测是插件的“核心技术肌肉”，但应当**以静默、精密的方式呈现**（如机械表的微雕刻度），而非全屏闪烁呼吸灯。
   - 默认状态下只展现纯粹的书签本体；当用户聚焦或悬浮时，才优雅透出网络拓扑与延迟数据。
3. **极速与键盘优先 (Frictionless Speed)**
   - 0 毫秒感知延迟，减少一切阻碍视线的层级遮挡。
   - 全键盘驱动（`/` 或 `Ctrl+K` 瞬间聚焦搜索，`Tab` / `↑↓` 快速选路，`Enter` 直达最优地址）。

---

## 2. 色彩系统与 3 套极简主题 (Color Tokens)

抛弃过度花哨的霓虹赛博色系，精选 3 套极具质感、高对比度且适合长时间注视的纯粹调色盘。

### 2.1 极简主题定义

#### 1. ⚪ **Ceramic Light (陶瓷素白 · 默认浅色)**
*灵感源自现代建筑与素烧陶瓷，温润、透气、干净利落。*
- **Canvas (背景)**: `#FAFAFA` (极浅暖灰，消除刺眼纯白反光)
- **Surface (卡片/弹窗)**: `#FFFFFF` (纯白卡片与浮层)
- **Subtle (输入框/次级背景)**: `#F4F4F5`
- **Border (边框分割)**: `#E4E4E7` (1px 极细分界线)
- **Text Primary (主文字)**: `#18181B` (极深炭黑，保证最佳可读性)
- **Text Secondary (次要文字)**: `#71717A` (中性灰)
- **Text Tertiary (微弱文字)**: `#A1A1AA` (占位符与微弱数据)
- **Accent (强调色)**: `#18181B` (极简深黑) 或 `#2563EB` (克莱因蓝)

#### 2. ⚫ **Obsidian Dark (深曜黑 · 默认深色)**
*灵感源自黑曜石与现代 IDE 终端界面，深邃纯净，无多余光污染。*
- **Canvas (背景)**: `#09090B` (近乎纯黑的底色)
- **Surface (卡片/弹窗)**: `#121215` (略高一层的实体卡片)
- **Subtle (输入框/次级背景)**: `#18181B`
- **Border (边框分割)**: `#27272A` (极细暗部线框)
- **Text Primary (主文字)**: `#F4F4F5` (高亮米白)
- **Text Secondary (次要文字)**: `#A1A1AA` (中灰说明)
- **Text Tertiary (微弱文字)**: `#52525B` (微弱等宽数据)
- **Accent (强调色)**: `#FAFAFA` (高对比纯白) 或 `#3B82F6` (冷调亮蓝)

#### 3. 📜 **Paper Sand (纸本暖沙 · 护眼阅读)**
*灵感源自再生纸与电子墨水屏，质朴、安宁。*
- **Canvas (背景)**: `#F5F2EB` (温润纸本底色)
- **Surface (卡片/弹窗)**: `#FAF8F5` (纯净纸面)
- **Subtle (输入框/次级背景)**: `#EAE5DB` (柔和凹陷底色)
- **Border (边框分割)**: `#DDD7CD` (柔和纸边框)
- **Text Primary (主文字)**: `#2D2B28` (胡桃木深灰褐)
- **Text Secondary (次要文字)**: `#756F68` (中性纸墨灰)
- **Text Tertiary (微弱文字)**: `#A8A196` (浅色字)
- **Accent (强调色)**: `#3A5A40` (沉稳墨绿)

---

### 2.2 状态与网络拓扑语义色 (Semantic Micro Colors)

状态色全部**降低饱和度、收敛面积**，仅作为 4px~6px 微型指示点或低对比度标签使用：

| 语义 | 作用 | 浅色值 (Light) | 深色值 (Dark) | 呈现方式 |
| :--- | :--- | :--- | :--- | :--- |
| **Intranet (内网最优)** | 命中最优局域网 IP / 低延迟 | `#16A34A` (Sage Green) | `#22C55E` | 4px 静音绿点 + 灰色等宽字 |
| **Extranet (公网备用)** | 走公网域名 / 云端出口 | `#2563EB` (Cobalt Blue) | `#38BDF8` | 4px 蓝点或灰色空心点 |
| **Warning (延迟偏高/重复)** | 探测耗时 > 500ms 或疑似重复 | `#D97706` (Muted Amber) | `#FBBF24` | 琥珀微标 |
| **Offline (不可达)** | 探测失败 / 离线 | `#DC2626` (Muted Red) | `#F87171` | 4px 灰色空心点或淡红标 |

---

### 2.3 完整的 CSS 自定义属性变量表 (CSS Variables)

```css
/* ==========================================================================
   Smart Bookmark - 极简主义设计变量系统 (CSS Design Tokens)
   ========================================================================== */

:root, [data-theme="obsidian-dark"] {
  /* 基础画布与容器 */
  --bg-canvas: #09090b;
  --bg-surface: #121215;
  --bg-surface-hover: #1c1c21;
  --bg-subtle: #18181b;
  --bg-input: #121215;

  /* 边框与分割线 */
  --border-subtle: #27272a;
  --border-focus: #52525b;

  /* 文字灰阶 */
  --text-primary: #f4f4f5;
  --text-secondary: #a1a1aa;
  --text-tertiary: #52525b;

  /* 品牌与强调色 */
  --accent: #f4f4f5;
  --accent-fg: #09090b;
  --accent-hover: #e4e4e7;

  /* 网络状态微标颜色 */
  --status-intranet: #22c55e;
  --status-extranet: #38bdf8;
  --status-warn: #fbbf24;
  --status-danger: #f87171;

  /* 阴影（极轻微，仅用于悬浮或弹窗） */
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.3);
  --shadow-md: 0 4px 12px 0 rgb(0 0 0 / 0.4);
  --shadow-popover: 0 12px 32px -4px rgb(0 0 0 / 0.6);

  /* 几何与度量 */
  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-full: 9999px;
}

[data-theme="ceramic-light"] {
  --bg-canvas: #fafafa;
  --bg-surface: #ffffff;
  --bg-surface-hover: #f4f4f5;
  --bg-subtle: #f4f4f5;
  --bg-input: #ffffff;

  --border-subtle: #e4e4e7;
  --border-focus: #a1a1aa;

  --text-primary: #18181b;
  --text-secondary: #71717a;
  --text-tertiary: #a1a1aa;

  --accent: #18181b;
  --accent-fg: #ffffff;
  --accent-hover: #27272a;

  --status-intranet: #16a34a;
  --status-extranet: #2563eb;
  --status-warn: #d97706;
  --status-danger: #dc2626;

  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 12px 0 rgb(0 0 0 / 0.06);
  --shadow-popover: 0 12px 32px -4px rgb(0 0 0 / 0.12);
}

[data-theme="paper-sand"] {
  --bg-canvas: #f5f2eb;
  --bg-surface: #faf8f5;
  --bg-surface-hover: #f0ebe1;
  --bg-subtle: #eae5db;
  --bg-input: #faf8f5;

  --border-subtle: #ddd7cd;
  --border-focus: #b5ada0;

  --text-primary: #2d2b28;
  --text-secondary: #756f68;
  --text-tertiary: #a8a196;

  --accent: #3a5a40;
  --accent-fg: #ffffff;
  --accent-hover: #2f4934;

  --status-intranet: #2d6a4f;
  --status-extranet: #2a6f97;
  --status-warn: #b07d2b;
  --status-danger: #b93c3c;

  --shadow-sm: 0 1px 2px 0 rgb(45 43 40 / 0.04);
  --shadow-md: 0 4px 12px 0 rgb(45 43 40 / 0.06);
  --shadow-popover: 0 12px 32px -4px rgb(45 43 40 / 0.1);
}
```

---

## 3. 字体排版与网格标尺 (Typography & Spacing)

### 3.1 字体栈定义

```css
/* 主界面字体：系统原生现代无衬线字体 */
--font-sans: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", Roboto, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;

/* 网络数据/时间/延迟/IP专用字体：严密对齐的等宽字体 */
--font-mono: "SF Mono", "JetBrains Mono", Menlo, Monaco, Consolas, "Courier New", monospace;
```

### 3.2 模块化字阶表 (Type Scale)

| 角色 (Role) | 字号 / 行高 | 字重 (Weight) | 字体族 | 适用场景 |
| :--- | :--- | :--- | :--- | :--- |
| **Hero Clock** | `44px ~ 52px` / `1.1` | `200` (Ultralight) / `300` (Light) | Mono | 顶部中心极简数字时钟（纤细无压迫感） |
| **Heading 1** | `16px ~ 18px` / `1.4` | `600` (Semibold) | Sans | 分组大标题、弹窗主标题 |
| **Body (Card Title)**| `13px ~ 14px` / `1.4` | `500` (Medium) | Sans | 书签名称、主输入框文字 |
| **Caption (Subtext)**| `12px` / `1.3` | `400` (Regular) | Sans | 书签副标题、标签筛选按钮、座右铭 |
| **Data (IP & Metric)**| `11px` / `1.2` | `400` (Regular) | Mono | IP 地址、延迟数字（如 `10.8.0.52 · 4ms`） |
| **Micro Badge** | `10px` / `1.0` | `500` (Medium) | Sans / Mono | 内网/外网极简文字微标 |

### 3.3 间距网格系统 (Spacing & Grid)

系统基于 **4px / 8px 基础网格系统**：
- **微间距**: `4px` (gap-1), `8px` (gap-2) —— 图标与文本、徽章内边距
- **组件间距**: `12px` (gap-3), `16px` (gap-4) —— 卡片内边距、搜索框内边距
- **区块留白**: `24px` (gap-6), `32px` (gap-8), `48px` (gap-12) —— 分组与分组之间、Hero 区域与内容区域之间

---

## 4. 界面版式架构与 ASCII 原型 (Layout Architecture)

### 4.1 新标签页总体布局 (Desktop NewTab)

```
┌──────────────────────────────────────────────────────────────────────────┐
│ [Logo] Smart Bookmark                           [+ 添加] [↓ 导入] [⚙ 设置] │ <- 极简顶栏(无厚底色)
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│                                14 : 28                                   │ <- 极细等宽时钟
│                        9月1日 星期二 · 杭州市                             │ <- 浅灰小字
│                                                                          │
│                 ┌──────────────────────────────────────┐                 │
│                 │ 🔍  搜索书签或按 Enter 百度/Google ↵ │                 │ <- 胶囊搜索框
│                 └──────────────────────────────────────┘                 │
│                                                                          │
│                   [全部]  [开发]  [运维]  [监控]  [日常]                   │ <- 纯纯文字药丸标签
│                                                                          │
│  ★ 常用置顶 ───────────────────────────────────────────────────────────  │ <- 0.5px 极细分割线
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│  │  GitLab     │ │ 󰒋 Jenkins    │ │ 󰄛 Grafana    │ │ 󰒋 Wiki       │    │
│  │ 10.8.0.52 4ms│ │ 10.8.0.60 8ms│ │ 10.8.0.88 2ms│ │ wiki.corp 12ms│   │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘    │
│                                                                          │
│  📂 研发基础服务 ──────────────────────────────────────────────────────  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│  │ 󰊤 Sentry     │ │ 󰄛 Prometheus │ │ 󰒋 Harbor     │ │ 󰄛 KubeSphere │    │
│  │ 10.8.1.10 3ms│ │ 10.8.1.12 5ms│ │ hub.corp 15ms│ │ 10.8.2.1 10ms│   │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘    │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

### 4.2 快捷弹窗布局 (Popup)

```
┌───────────────────────────────────────────────┐
│ 🔍  快速搜索或直接访问...             [⚙]      │ <- 紧凑输入行
├───────────────────────────────────────────────┤
│ 常用直达                                       │
│ ┌───────────────────────────────────────────┐ │
│ │  GitLab              ● 10.8.0.52     4ms │ │
│ ├───────────────────────────────────────────┤ │
│ │ 󰒋 Jenkins             ● 10.8.0.60     8ms │ │
│ ├───────────────────────────────────────────┤ │
│ │ 󰄛 Grafana             ● 10.8.0.88     2ms │ │
│ ├───────────────────────────────────────────┤ │
│ │ 󰒋 知识库 Wiki         ○ wiki.corp    12ms │ │
│ └───────────────────────────────────────────┘ │
│                                               │
│ [↑↓] 选择   [↵] 最优直达   [Tab] 展开所有入口   │ <- 底部单行快捷键提示
└───────────────────────────────────────────────┘
```

---

## 5. 核心组件精细化设计规范 (Component Specifications)

### 5.1 顶部静音导航条 (Top Navigation Bar)
- **视觉特征**：背景为极低饱和半透明底色，向下滚动时产生 `backdrop-filter: blur(16px)` 与 `1px solid var(--border-subtle)`。
- **Logo 区域**：去除复杂渐变背景，采用单色简约书签矢量图标 + `13px` Medium 灰度文字。
- **操作按钮**：
  - 去除厚重的大彩块。
  - 主要操作（“+ 添加书签”）：采用**高对比度小胶囊按钮**（浅色下为纯黑底白字，深色下为纯白底黑字，`h: 32px`, `px: 14px`, `rounded-full`, `text: 12px`）。
  - 次要操作（“导入”、“设置”）：采用**无边框纯图标/文字按钮**，悬浮时背景浅灰微亮，保持界面极度清爽。
  - 清空数据等危险操作：从顶栏移除，归入设置面板二级操作区。

---

### 5.2 极简中心搜索条 (Search Capsule)
- **形态**：单行极简胶囊，`h: 46px`，`rounded-full`。
- **边框与阴影**：
  - 默认态：`1px solid var(--border-subtle)`，背景为 `var(--bg-surface)`，无重投影。
  - 聚焦态：`1px solid var(--border-focus)`，轻微提升 `var(--shadow-md)`。
- **搜索引擎切换**：
  - 输入框左侧内嵌微型单色引擎 Icon（Google / 百度 / Bing / GitHub），点击原位滑出单色下拉面板。
- **右侧快捷键提示**：
  - 优雅的单色等宽小标签 `<kbd>⌘K</kbd>` 或 `<kbd>/</kbd>`，字号 `10px`，颜色 `--text-tertiary`。

---

### 5.3 签名级特色组件：精密寻径书签卡片 (The Precision Route Card)

这是整个界面的核心记忆点：**“外表极简克制，内里极具机械精密度。”**

#### A. 卡片外观解构 (Card Anatomy)

```
┌────────────────────────────────────────────────────────┐
│  [ 图标 ]  书签主标题                         [ ⋮ 更多 ]│
│  26x26    13px Medium 字重                   悬浮才显现│
│                                                        │
│  ● 10.8.0.52                                       4ms │ <- 等宽网络拓扑信息行
│  ↑                                                 ↑   │
│ 4px绿点(内网)                                  精确实测延迟 │
└────────────────────────────────────────────────────────┘
```

- **容器规格**：
  - 圆角：`rounded-lg` (8px)，去除过于圆润的 16px，显得更专业干练。
  - 边框：`1px solid var(--border-subtle)`。
  - 背景：`var(--bg-surface)`。
  - 悬浮反馈（Hover）：卡片不产生大幅度的上浮位移或刺眼发光，仅发生 **边框颜色微加深 + 背景微提亮 + 右上角微操作图标淡入**。
- **图标展示 (Icon)**：
  - 尺寸统一为 `26px × 26px`，采用单色 SVG 或抓取的真实网站高清 Favicon。
  - 图标底色为浅灰色圆角矩形，防止白底或异形图标破坏整体统一感。
- **路由数据行 (Route Info Footer)**：
  - 严格采用 `--font-mono` 等宽字体，字号 `11px`，颜色 `--text-secondary`。
  - 当命中内网最长前缀时：显示 `● 10.8.0.52 · 4ms`（4px 绿色实心微点）。
  - 当走公网备用时：显示 `○ gitlab.com · 35ms`（4px 灰色空心点）。

#### B. 悬浮展开与多入口切换 (Dropdown on Hover/Click)
当用户点击卡片右侧更多或展开时，向下平滑展开极简清单：
```
┌───────────────────────────────────────────────┐
│ ✓ ● 10.8.0.52 (内网机房A)         4ms  [默认]  │
│   ○ 192.168.1.100 (内网机房B)    12ms  [直达]  │
│   ○ git.company.com (公网VPN)    48ms  [直达]  │
└───────────────────────────────────────────────┘
```

---

### 5.4 分组与标签栏 (Groups & Tag Pills)
- **分组标题栏 (Group Header)**：
  - 采用 `14px` Semibold 标题，前面可带一个极简微图标或折叠指示符。
  - 右侧为一条平直的 `1px` 极细分割线贯穿至右边界，构成清爽的视觉隔断。
- **高频标签筛选条 (Tag Filter Pills)**：
  - 标签以纯文字小胶囊展现，默认字色 `--text-secondary`，悬浮时背景轻微灰底。
  - 激活态：深底高亮（黑底白字或白底黑字），`h: 26px`，`px: 10px`，字号 `11px`。

---

### 5.5 弹窗与抽屉规范 (Modal & Sheet Design)
- **遮罩层 (Backdrop)**：`bg-black/30 backdrop-blur-sm`，放弃 `bg-black/80` 的压抑深黑，保持背景隐约可见的透气感。
- **窗口容器**：
  - 宽度限制在 `480px ~ 560px`，统一基准高度 `530px`（`h-[530px] flex flex-col`），多 Tab 切换完全恒定一致无抖动。
  - 头部与底部取消粗厚的分界线，改用纯粹的内边距空间分割。
- **表单输入组件**：
  - 输入框高度 `36px`，底色 `var(--bg-subtle)`，无边框或单线极细框，聚焦时边框高亮。
  - 标签（Label）放在输入框上方，字号 `12px`，字重 `500`，字色 `--text-secondary`。
  - 多入口配置列表：采用极简条目卡片排版，点击“+ 添加入口”新增一行，内网/外网自动探测打标。
- **书签导入分组策略 (3 种目标分组模式规范)**：
  1. **按原路径设置分组名 (Path-based Grouping)**：自动解析 Chrome 导出的 HTML 书签树或浏览器书签目录层级，保留/自动创建同名分组。
  2. **指定固定分组 (Specified Group)**：
     - **约束**：**下拉选择器必须严格过滤掉「常用」与「未分组」**。
     - **原理**：「常用」为使用频率最高的动态聚合组，「未分组」为无归属书签的自动计算组；指定分组仅支持用户自定义创建的具体分组。
     - **交互**：若用户暂无任何自定义分组，给予提示引导前往偏好设置创建或改用路径/未分组策略。
  3. **存入未分组 (Ungrouped)**：直接归入系统默认的「未分组」中。

---

### 5.6 提示反馈 (Toast & Empty States)
- **极简 Toast**：
  - 触发智能跳转时，在屏幕底部中央弹出极简单色提示胶囊：
  - `⚡ 内网直达: 10.8.0.52 (4ms)`，停留 1.5 秒后静默淡出。
- **空状态 (Empty State)**：
  - 无繁复插画，仅居中放置单色极简线框图标 + 一行 13px 提示文字 + 醒目的小胶囊操作按钮。

---

### 5.7 自适应表单选择器规范 (Adaptive Form Select & Segmented Control)

针对弹窗表单（设置、书签新增/编辑、导入迁移、Popup）中的单项选择控件，**彻底废除浏览器原生 `<select>` 标签**（原生下拉菜单在跨系统下无法换肤、黑白对比失调且无法展示微标），统一实施**智能自适应选择器体系**：

#### A. 选项计数自适应判定规则 (The Adaptive Threshold Rule)
- **当选项数 $N \le 2$ 时（二选一/开关型）**：
  - **采用形态**：**iOS/macOS 风格极简分段胶囊 (Segmented Control)**。
  - **设计目标**：**零下拉层级、单次点击直接切换**。选项全部平铺在带有 `bg-subtle` 的浅底凹槽内，当前选中项呈现浮起卡片质感 (`bg-surface` + 微弱投影 `shadow-sm`)。
  - **典型应用**：时钟制式（`24 小时制` vs `12 小时制`）、布尔策略开关。
  ```text
  时钟制式 (N=2):
  ┌───────────────────────────────┬───────────────────────────────┐
  │     ● 24 小时制 (14:30)       │        12 小时制 (02:30)      │
  └───────────────────────────────┴───────────────────────────────┘
  ```

- **当选项数 $N > 2$ 时（多选项/动态列表）**：
  - **采用形态**：**Linear / Raycast 风格极简卡片弹出选择器 (Custom Popover Select)**。
  - **设计目标**：**主题 100% 深度融合、支持图标/徽标与精密勾选状态**。
  - **交互特征**：
    1. **触发器 (Trigger)**：高度 `36px`，`bg-subtle` 底色 + `1px solid var(--border-subtle)`，右侧为顺滑翻转的微型指示箭头（展开时平滑旋转 180°）。
    2. **下拉卡片 (Popover)**：`bg-surface` 实体背景 + `var(--shadow-popover)` 柔和弥散阴影，`border-radius: 10px`。
    3. **条目设计 (Item)**：支持左侧图标/色彩微标展示，当前选中项右侧显示单色勾选图标（`✓`），鼠标悬停呈现 `bg-subtle` 平滑过渡。
    4. **失焦关闭**：点击页面空白外部区域或按下 `Esc` 键时即刻平滑收起。
  - **典型应用**：所属分组选择（多分组）、默认主搜索引擎选择（百度/谷歌/必应/DuckDuckGo）、目标导入分组等。
  ```text
  默认主搜索引擎 (N=4):
  ┌─────────────────────────────────────────────────────────────┐
  │ 🌐 谷歌 (Google)                                          ▾ │
  └──────────────────────────────┬──────────────────────────────┘
                                 │ ┌──────────────────────────┐
                                 │ │   ● 百度 (Baidu)         │
                                 │ │ ✓ 🌐 谷歌 (Google)     ✓ │
                                 │ │   ⚡ 必应 (Bing)          │
                                 │ │   🦆 DuckDuckGo          │
                                 │ └──────────────────────────┘
  ```

---

## 6. 交互动效与微反馈 (Motion & Micro-interactions)

极简风格排斥一切拖泥带水的花哨动画，所有动效以“**干脆、轻盈、物理直觉**”为准则：

1. **状态过渡时间 (Duration)**：
   - 按钮悬浮、颜色切换：`120ms ease-out`
   - 下拉菜单展开、Toast 提示：`160ms cubic-bezier(0.16, 1, 0.3, 1)`
   - 弹窗呼出：`200ms cubic-bezier(0.16, 1, 0.3, 1)`（伴随微小的 `scale(0.98 -> 1)` 物理形变）
2. **拒绝动画干扰**：
   - 状态呼吸灯**不再常驻剧烈缩放放大**，改用纯色静态显示或极其轻微的透明度脉冲（0.7 -> 1.0），避免新标签页看起来像闪烁的广告牌。
3. **快捷键系统**：
   - `/` 或 `Ctrl/Cmd + K`：聚焦搜索框
   - `Esc`：关闭任何打开的弹窗 / 取消搜索聚焦
   - `Enter`：当有书签匹配时跳往最优地址；无匹配时调起外部搜索引擎

---

## 7. 现有代码重构与落地对照表 (Refactoring Checklist)

| 模块 / 页面 | 当前问题 | 改造动作 |
| :--- | :--- | :--- |
| **全局 CSS (`newtab.css`)** | 包含 5 套复杂的深红、霓虹粉、大绿主题，变量命名散乱 | 精简为 `ceramic-light` (默认浅色)、`obsidian-dark` (默认深色)、`paper-sand` 3 套主题，统一定义极简语义 Token。 |
| **顶栏导航 (`newtab.html`)** | 顶部包含醒目的红色“清空数据”大按钮，按钮视觉过重 | “清空数据”移入设置面板二级菜单；顶栏按钮精简为极简图标与单色胶囊按钮。 |
| **时钟与美化区** | 时钟字体较粗，座右铭带有醒目的黄色五角星，视觉分散 | 时钟改用 `font-weight: 200` 极细等宽字体；座右铭精简为居中单行低对比度灰字。 |
| **搜索框** | 边框较厚，有大阴影，下拉框有过多层级 | 重构为单线胶囊搜索条，聚焦时轻微提亮，去除多余投影与装饰边框。 |
| **书签卡片** | 圆角过大，内网/外网徽章色彩过于艳丽，hover 效果浮夸 | 卡片圆角缩减为 `8px`，使用细边框与低饱和度背景；将 IP 与延迟用极小等宽字体排版在底部。 |
| **主题管理脚本 (`theme.js`)** | 兼容了旧的 5 套主题选择与冗余的切换逻辑 | 升级为 3 套极简主题的无缝切换，并支持系统深浅色偏好跟随 (`prefers-color-scheme`)。 |
| **Popup 弹窗** | 弹窗内控件密集，列表行高低不一 | 采用 360px 宽度统一列表流，左侧图标 + 书签名称，右侧精密等宽延迟微标，点击即达。 |
