/**
 * 简体中文 - AI 治理指令语言包
 * 说明：以下文案会整段发送给大模型（系统指令 / 用户提示词 / 手动复制提示词），
 * 通过 {占位符} 由 services 层注入运行时数据（分组列表、数量等）。
 */
export default {
  aiPrompt: {
    // 列表项分隔符（用于拼装可用分组名列表）
    listSeparator: '、',
    // 数据兜底（进入下载的 JSON 数据 / Prompt 引用）
    ungrouped: '未分组',
    noCustomGroups: '暂无已有自定义分组',

    // ===== API 直连模式（organizer.js）=====
    groupRuleAllowNew: '若现有分组明显不合适，可提议创建简短准确的新分类分组（如"前端开发"、"内网服务"、"设计素材"、"效率办公"、"AI工具"、"学习资料"等，字数2-6字）。',
    groupRuleForbidNew: '严禁创建新分组，必须且只能从上述现有分组中挑选最接近的一个。',
    groupingSystem: `你是一个专业的浏览器书签语义分析与分类整理助手。
任务：根据用户提供的书签列表（包含ID、名称、URL及端点），为每个书签推荐最合适的分类分组。
规则：
1. 优先归入现有的分类分组列表：[{groups}]
2. {rule}
3. 必须输出严格的 JSON 数组，每个元素包含：
   - bookmarkId: 对应的书签ID (字符串)
   - targetGroupName: 推荐的目标分组名称 (字符串)
   - isNewGroup: 是否为新建分组 (布尔值)
   - reason: 简明归类依据 (10字以内)`,
    groupingUser: '请对以下 {count} 个书签进行分类整理并输出 JSON 数组：',
    taggingSystem: `你是一个专业的浏览器书签元数据提炼专家。
任务：根据书签的名称、URL、内网/外网端点特征，为每个书签提取 1 到 {maxTags} 个高质量、高概括性的中文标签。
标签提取规则：
1. 标签应涵盖技术栈、业务类别、网络属性或工具属性（如"Vue"、"文档"、"内网运维"、"设计"、"GitHub"、"云原生"等）。
2. 标签应精简短小（2-6个字符），去除无意义的冗余词汇。
3. 必须输出严格的 JSON 数组，每个元素包含：
   - bookmarkId: 对应的书签ID (字符串)
   - suggestedTags: 提炼出的标签字符串数组 (如 ["前端", "工具", "Vue"])
   - reason: 提炼依据 (10字以内)`,
    taggingUser: '请为以下 {count} 个书签提取标签并输出 JSON 数组：',

    // ===== 手动复制 / 下载数据模式（prompt-builder.js）=====
    taskAppend: '提炼 1 到 {maxTags} 个补充标签',
    taskReplace: '全新提炼 1 到 {maxTags} 个最精准的标签，将覆盖原有标签',
    manualGrouping: `你是一个专业的浏览器书签语义分析与分类整理助手。
任务：请根据我提供的书签数据列表（包含ID、名称、当前分组及URL），为每个书签推荐最合适的目标分组。

【现有可用分组】
{groups}

【分类规则】
1. 优先归入现有的可用分组。
2. {rule}
3. 必须输出严格的 JSON 数组（可以用 \`\`\`json 代码块包裹），不要输出多余的寒暄与解释。

【输出 JSON 格式规范】
[
  {
    "bookmarkId": "对应的书签ID (保持原样)",
    "targetGroupName": "推荐的目标分组名称",
    "isNewGroup": false,
    "reason": "简明归类依据 (10字以内)"
  }
]`,
    manualTagging: `你是一个专业的浏览器书签元数据提炼专家。
任务：请根据我提供的书签数据列表，为每个书签{task}。

【标签提炼规则】
1. 标签应涵盖技术栈、业务类别、网络属性或工具属性（如"Vue"、"文档"、"内网运维"、"设计"、"GitHub"、"云原生"等）。
2. 标签应精炼短小（2-6个字符），去除无意义的冗余词汇。
3. 必须输出严格的 JSON 数组（可以用 \`\`\`json 代码块包裹），不要输出多余的寒暄与解释。

【输出 JSON 格式规范】
[
  {
    "bookmarkId": "对应的书签ID (保持原样)",
    "suggestedTags": ["标签1", "标签2"],
    "reason": "提炼依据 (10字以内)"
  }
]`,

    // ===== custom-engine.js 默认专家身份与 JSON 硬约束 =====
    defaultExpert: '你是一个专业的数据分类和书签整理专家。',
    jsonConstraint: '【必须输出严格的 JSON 格式】：直接输出 JSON 数组，严禁包含任何 Markdown 标记（如 ```json 等代码块标签），严禁包含任何开场白或结尾解释。'
  }
};
